Review model test documentation

There is an initial initialization test to ensure objects are created correctly.

There is an edge case for if mandatory fields are missing and raise an error

Another edge case for if there is a rating and an empty comment. It should still be accepted

There is a functional test that ensures ids are stored as integers for review

Another functional test is made to ensure customer reviews are visible to users
